@extends('layouts.app', ['title'=>'Licencias','heading'=>'Licencias'])
@section('content') @include('partials.flash')<div class="d-flex justify-content-end mb-3"><form method="POST" action="{{ route('licenses.store') }}">@csrf<button class="btn btn-dark">+ Crear código único</button></form></div><div class="panel p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Código</th><th>Estado</th><th>Creada por</th><th>Creada</th><th>Activación</th></tr></thead><tbody>@forelse($items as $item)<tr><td class="font-monospace fw-bold">{{ $item->code }}</td><td>{{ ucfirst($item->status) }}</td><td>{{ $item->creator->name }}</td><td>{{ $item->created_at->format('d/m/Y H:i') }}</td><td>{{ $item->activated_at?->format('d/m/Y H:i') ?? 'Sin activar' }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">No hay licencias.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>@endsection
