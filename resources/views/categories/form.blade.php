@extends('layouts.app', ['title' => $item->exists ? 'Editar categoría' : 'Nueva categoría', 'heading' => $item->exists ? 'Editar categoría' : 'Nueva categoría'])
@section('content') @include('partials.flash')<div class="panel p-4" style="max-width: 600px"><form method="POST" action="{{ $item->exists ? route('categories.update', $item) : route('categories.store') }}">@csrf @if($item->exists) @method('PUT') @endif<div class="mb-3"><label class="form-label">Nombre</label><input name="name" class="form-control" value="{{ old('name', $item->name) }}" required></div><div class="form-check"><input type="checkbox" name="is_active" value="1" class="form-check-input" id="active" @checked(old('is_active', $item->is_active ?? true))><label class="form-check-label" for="active">Activa</label></div><button class="btn btn-dark mt-4">Guardar</button><a href="{{ route('categories.index') }}" class="btn btn-light mt-4">Cancelar</a></form></div>@endsection
