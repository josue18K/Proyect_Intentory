@extends('layouts.app', ['title' => 'Registrar movimiento', 'heading' => 'Registrar movimiento'])
@section('content') @include('partials.flash')<div class="panel p-4" style="max-width: 700px"><form method="POST" action="{{ route('movements.store') }}">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label">Producto</label><select name="product_id" class="form-select" required><option value="">Selecciona</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(old('product_id')==$product->id)>{{ $product->internal_code }} - {{ $product->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Sede</label><select name="branch_id" class="form-select" required><option value="">Selecciona</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(old('branch_id')==$branch->id)>{{ $branch->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Tipo</label><select name="type" class="form-select" required><option value="entrada" @selected(old('type','entrada')==='entrada')>Entrada / reingreso</option><option value="salida" @selected(old('type')==='salida')>Salida</option></select></div><div class="col-md-4"><label class="form-label">Cantidad</label><input name="quantity" type="number" min="1" class="form-control" value="{{ old('quantity') }}" required></div><div class="col-md-4"><label class="form-label">Fecha del movimiento</label><input name="movement_date" type="date" class="form-control" value="{{ old('movement_date', now()->format('Y-m-d')) }}" max="{{ now()->format('Y-m-d') }}" required></div><div class="col-12"><label class="form-label">Motivo</label><input name="reason" class="form-control" value="{{ old('reason') }}" placeholder="Compra, reingreso, venta, daño..."></div><div class="col-12"><label class="form-label">Notas</label><textarea name="notes" class="form-control">{{ old('notes') }}</textarea></div></div><div class="form-text mt-3">Si una salida deja el stock en cero, se guardará la fecha de agotamiento. Un reingreso registra la nueva fecha de ingreso y reinicia el estado de agotado.</div><button class="btn btn-dark mt-4">Registrar</button><a href="{{ route('movements.index') }}" class="btn btn-light mt-4">Cancelar</a></form></div>@endsection
