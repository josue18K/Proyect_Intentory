@extends('layouts.app', ['title'=>'Auditoría','heading'=>'Historial y auditoría'])
@section('content')<form class="mb-3 d-flex gap-2"><input name="action" value="{{ request('action') }}" class="form-control" placeholder="Filtrar por acción"><button class="btn btn-outline-dark">Filtrar</button></form><div class="panel p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Fecha</th><th>Usuario</th><th>Acción</th><th>Datos nuevos</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->created_at->format('d/m/Y H:i') }}</td><td>{{ $item->user?->name ?? 'Sistema' }}</td><td>{{ $item->action }}</td><td><code>{{ json_encode($item->new_values, JSON_UNESCAPED_UNICODE) }}</code></td></tr>@empty<tr><td colspan="4" class="text-center text-muted">No hay registros.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>@endsection
