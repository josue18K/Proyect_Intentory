@extends('layouts.app',['title'=>'Productos','heading'=>'Productos por tienda'])
@section('content') @include('partials.flash')
<div class="module-intro mb-4"><div><span class="eyebrow-dark">Catálogo por sede</span><h2>Productos</h2><p>Consulta códigos, precios y cantidades de cada tienda.</p></div><div class="module-actions"><span class="record-count"><strong>{{ $items->total() }}</strong><small>registros</small></span>@if(auth()->user()->role==='administrador')<a class="btn btn-dark" href="{{ route('products.create') }}">+ Nuevo producto</a>@endif</div></div>
<form class="panel p-3 mb-3 row g-2"><div class="col-md-5"><input name="search" class="form-control" value="{{ request('search') }}" placeholder="Nombre, código, categoría o código de barras"></div><div class="col-md-4"><select name="branch_id" class="form-select"><option value="">Todas las tiendas</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(request('branch_id')==$branch->id)>{{ $branch->name }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-dark flex-grow-1">Filtrar</button><a class="btn btn-light" href="{{ route('products.index') }}">Limpiar</a></div></form>
<div class="panel p-3"><div class="table-responsive"><table class="table"><thead><tr><th>Sede</th><th>Código</th><th>Producto</th><th>Categoría</th><th>Precio</th><th>Cantidad</th><th></th></tr></thead><tbody>@forelse($items as $item) @php($stock=$item->inventories->firstWhere('branch_id',$item->branch_id)?->quantity??0)<tr><td><span class="badge badge-normal">{{ $item->branch?->name??'Sin sede' }}</span></td><td>{{ $item->internal_code }}<div class="small text-muted">{{ $item->barcode }}</div></td><td><strong>{{ $item->name }}</strong><div><a class="small" href="{{ route('products.history',$item) }}">Ver historial</a></div></td><td>{{ $item->category->name }}</td><td>S/ {{ number_format($item->sale_price,2) }}</td><td><strong>{{ $stock }}</strong> @if($stock<=$item->minimum_stock)<span class="badge badge-low">Bajo</span>@endif</td><td class="actions-cell">@if(auth()->user()->role==='administrador')<a class="btn btn-sm btn-outline-dark" href="{{ route('products.edit',$item) }}">Editar</a><form class="d-inline" method="POST" action="{{ route('products.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('¿Desactivar producto?')">Eliminar</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="text-center text-muted">No hay productos para estos filtros.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>
@endsection
