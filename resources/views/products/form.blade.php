@extends('layouts.app',['title'=>$item->exists?'Editar producto':'Nuevo producto','heading'=>$item->exists?'Editar producto':'Nuevo producto'])
@section('content') @include('partials.flash')<div class="panel p-4"><form method="POST" action="{{ $item->exists?route('products.update',$item):route('products.store') }}">@csrf @if($item->exists)@method('PUT')@endif<div class="row g-3"><div class="col-md-4"><label class="form-label">Tienda</label><select name="branch_id" class="form-select" required>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(old('branch_id',$item->branch_id)==$branch->id)>{{ $branch->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Código interno por tienda</label><input name="internal_code" class="form-control" value="{{ old('internal_code',$item->internal_code) }}" placeholder="Automático si se deja vacío"><div class="form-text">Solo debe ser único dentro de la tienda seleccionada.</div></div><div class="col-md-4"><label class="form-label">Código de barras</label><div class="input-group"><input id="barcode" name="barcode" class="form-control" value="{{ old('barcode',$item->barcode) }}"><button type="button" class="btn btn-outline-dark" data-barcode-scan>Escanear</button></div></div><div class="col-md-6"><label class="form-label">Nombre</label><input name="name" class="form-control" value="{{ old('name',$item->name) }}" required></div><div class="col-md-3"><label class="form-label">Categoría</label><select name="category_id" class="form-select" required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id',$item->category_id)==$category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Precio (S/)</label><input name="sale_price" type="number" step=".01" min="0" class="form-control" value="{{ old('sale_price',$item->sale_price??0) }}" required></div><div class="col-md-3"><label class="form-label">Stock mínimo</label><input name="minimum_stock" type="number" min="0" class="form-control" value="{{ old('minimum_stock',$item->minimum_stock??0) }}" required></div>@unless($item->exists)<div class="col-md-3"><label class="form-label">Cantidad inicial</label><input name="initial_quantity" type="number" min="0" class="form-control" value="{{ old('initial_quantity',0) }}" required></div>@endunless<div class="col-12"><label class="form-label">Descripción</label><textarea name="description" class="form-control">{{ old('description',$item->description) }}</textarea></div></div><div class="mt-4"><button class="btn btn-dark">Guardar producto</button><a class="btn btn-light" href="{{ route('products.index') }}">Cancelar</a></div></form></div><div class="modal fade" id="barcodeModal" tabindex="-1"><div class="modal-dialog modal-dialog-centered"><div class="modal-content"><div class="modal-header"><h5 class="modal-title">Escanear código</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><video data-barcode-video class="w-100 rounded bg-dark" style="min-height:220px" autoplay playsinline></video><p data-barcode-status class="small text-muted mt-2">Apunta la cámara al código de barras.</p></div></div></div></div>@endsection
