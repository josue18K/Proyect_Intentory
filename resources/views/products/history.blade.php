@extends('layouts.app', ['title'=>'Historial','heading'=>'Historial: '.$product->name])
@section('content')
@include('partials.flash')
<div class="panel p-3"><div class="d-flex justify-content-between mb-3"><div><strong>{{ $product->internal_code }}</strong><div class="text-muted">Registro de entradas, reingresos y salidas</div></div><a class="btn btn-outline-dark" href="{{ route('products.index') }}">Volver</a></div><div class="table-responsive"><table class="table"><thead><tr><th>Fecha del movimiento</th><th>Sede</th><th>Tipo</th><th>Cantidad</th><th>Antes</th><th>Después</th><th>Usuario</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ ($item->movement_date ?? $item->created_at)->format('d/m/Y') }}<div class="text-muted small">Registrado {{ $item->created_at->format('d/m/Y H:i') }}</div></td><td>{{ $item->branch->name }}</td><td>{{ ucfirst($item->type) }}</td><td>{{ $item->quantity }}</td><td>{{ $item->stock_before }}</td><td>{{ $item->stock_after }} @if($item->stock_after === 0)<span class="badge badge-empty">Agotado</span>@endif</td><td>{{ $item->user->name }}</td></tr>@empty<tr><td colspan="7" class="text-center text-muted">No hay historial.</td>@endforelse</tbody></table></div>{{ $items->links() }}</div>
@endsection
