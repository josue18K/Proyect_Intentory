@extends('layouts.app', ['title'=>'Transferencias','heading'=>'Transferencias entre sedes'])
@section('content') @include('partials.flash')
<div class="d-flex justify-content-between mb-3"><form class="d-flex gap-2"><select name="status" class="form-select"><option value="">Todos los estados</option><option value="pending">Pendientes</option><option value="completed">Completadas</option><option value="cancelled">Canceladas</option></select><button class="btn btn-outline-dark">Filtrar</button></form><a class="btn btn-dark" href="{{ route('transfers.create') }}">+ Nueva transferencia</a></div>
<div class="panel p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Fecha</th><th>Producto</th><th>Origen</th><th>Destino</th><th>Cantidad</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->created_at->format('d/m/Y H:i') }}</td><td>{{ $item->product->name }}</td><td>{{ $item->fromBranch->name }}</td><td>{{ $item->toBranch->name }}</td><td>{{ $item->quantity }}</td><td><span class="badge {{ $item->status === 'completed' ? 'badge-normal' : ($item->status === 'pending' ? 'badge-low' : 'bg-secondary') }}">{{ ['pending'=>'Pendiente','completed'=>'Completada','cancelled'=>'Cancelada'][$item->status] }}</span></td><td class="text-end">@if($item->status === 'pending')<form class="d-inline" method="POST" action="{{ route('transfers.complete',$item) }}">@csrf<button class="btn btn-sm btn-success">Completar</button></form><form class="d-inline" method="POST" action="{{ route('transfers.cancel',$item) }}">@csrf<button class="btn btn-sm btn-outline-danger">Cancelar</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="text-center text-muted">No hay transferencias.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>@endsection
