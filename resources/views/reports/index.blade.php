@extends('layouts.app',['title'=>'Reportes','heading'=>'Reportes de inventario']) @section('content') @include('partials.flash')
<form class="panel p-3 mb-3 row g-2"><div class="col-md-3"><select name="branch_id" class="form-select" required><option value="">Seleccionar sede</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(request('branch_id')==$branch->id)>{{ $branch->name }}</option>@endforeach</select></div><div class="col-md-2"><select name="type" class="form-select"><option value="">Todos</option><option value="entrada" @selected(request('type')==='entrada')>Entradas</option><option value="salida" @selected(request('type')==='salida')>Salidas</option></select></div><div class="col-md-2"><input type="date" name="from" class="form-control" value="{{ request('from') }}"></div><div class="col-md-2"><input type="date" name="to" class="form-control" value="{{ request('to') }}"></div><div class="col-md-3"><button class="btn btn-dark w-100">Consultar</button></div></form>
@if($selectedBranch)<div class="d-flex gap-2 flex-wrap mb-3"><a class="btn btn-outline-dark" href="{{ route('reports.csv',request()->query()) }}">Descargar CSV</a><a class="btn btn-dark" href="{{ route('reports.pdf',request()->query()) }}">Descargar PDF</a><button class="btn btn-success" data-whatsapp-url="{{ 'https://wa.me/?text='.rawurlencode('Reporte de inventario '.$selectedBranch->name.': '.$shareUrl) }}">Enviar PDF por WhatsApp</button></div><div class="row g-3"><div class="col-lg-7"><div class="panel p-3"><h2 class="h6">Existencias</h2><div class="table-responsive"><table class="table"><thead><tr><th>Producto</th><th>Código</th><th>Cantidad</th><th>Precio</th></tr></thead><tbody>@foreach($inventory as $row)<tr><td>{{ $row->product->name }}</td><td>{{ $row->product->internal_code }}</td><td>{{ $row->quantity }}</td><td>S/ {{ number_format($row->product->sale_price,2) }}</td></tr>@endforeach</tbody></table></div></div></div><div class="col-lg-5"><div class="panel p-3"><h2 class="h6">Mayor rotación por unidades (salidas)</h2><div class="table-responsive"><table class="table"><thead><tr><th>Producto</th><th>Unidades</th></tr></thead><tbody>@foreach($products as $product)<tr><td>{{ $product->name }}</td><td>{{ $product->rotation_units??0 }}</td></tr>@endforeach</tbody></table></div></div></div></div>@else<div class="alert alert-warning">Selecciona una sede para generar el reporte.</div>@endif @endsection
